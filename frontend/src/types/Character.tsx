export type Character = {
  id: string;
  name: string;
  description: string;
  occupation: string;
  age: number;
  budgetModifier: number; // Percentage modifier to apply to base budget
  image: string;
  backstory: string;
};
